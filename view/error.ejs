<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Shortened URL | Shortly</title>
    <style>
      body {
        font-family: "Poppins", sans-serif;
        background: linear-gradient(to right, #535353, #ffffff);
        margin: 0;
        padding: 0;
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        color: #333;
      }

      .container {
        width: 100%;
        max-width: 500px;
        background: white;
        padding: 25px;
        border-radius: 10px;
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        text-align: center;
      }

      h1 {
        color: #d32f2f;
        margin-bottom: 15px;
      }

      p {
        font-size: 16px;
        color: #555;
      }

      .error-message {
        color: #d32f2f;
        background-color: #ffebee;
        border: 1px solid #d32f2f;
        padding: 12px;
        border-radius: 5px;
        font-size: 16px;
        font-weight: bold;
        margin: 15px 0;
        box-shadow: 0px 2px 5px rgba(0, 0, 0, 0.2);
      }

      .back-link {
        display: inline-block;
        text-decoration: none;
        font-size: 14px;
        color: white;
        background: #007bff;
        padding: 10px 15px;
        border-radius: 5px;
        transition: 0.3s ease-in-out;
      }

      .back-link:hover {
        background: #0056b3;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1>Error</h1>

      <% if (message) { %>
      <p class="error-message"><%= message %></p>
      <a href="<%= goBack %>" class="back-link">← Go Back</a>
      <% } %>
    </div>
  </body>
</html>
